{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "types": "tsc -noEmit",
    "prebuild": "npm run types",
    "watch:build": "parcel watch src/index.html -d build",
    "watch:types": "tsc -noEmit -watch",
    "watch:serve": "http-server build -o",
    "start": "run-p watch:*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.2",
    "pixi.js": "^5.0.0-rc.2",
    "typescript": "^3.3.3333"
  }
}
